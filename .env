# Default Compose profile.
# Switch with: COMPOSE_PROFILES=hdfs docker-compose up -d
COMPOSE_PROFILES=s3

# Shared dev credentials (used by mysql, minio, web, etc.)
DEV_USER=admin
DEV_PASSWORD=admin123
